<div class="container">
  <p id="notice"><%= notice %></p>

  <h1>Our Tutorials</h1>

  <table>
    <thead>
      <tr>
        <th>Title</th>
        <th>Content</th>
        <th>Date</th>
        <th>Videourl</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @tutorials.each do |tutorial| %>
        <tr>
          <td><%= tutorial.title %></td>
          <td><%= simple_format(tutorial.content) %></td>
          <td><%= tutorial.date %></td>
          <td><%= tutorial.videoURL %></td>
          <td><%= link_to 'Show', tutorial %></td>
          <td><%= link_to 'Edit', edit_tutorial_path(tutorial) %></td>
          <td><%= link_to 'Destroy', tutorial, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <br>

  <%= link_to 'New Tutorial', new_tutorial_path %>
</div>
